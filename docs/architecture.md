# Architecture

The system follows a modular object-oriented design.

Main components:

- ConsoleUI → handles user interaction
- Library → manages books and borrowing rules
- UserStore → manages member accounts
- Book → represents book objects
- Member / Admin → user roles

Each component has a specific responsibility.
